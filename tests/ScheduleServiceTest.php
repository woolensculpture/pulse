<?php

use WITR\Schedule\ScheduleService;
use Carbon\Carbon;

class ScheduleServiceTest extends TestCase {

	/** @test */
	/*public function it_should_get_the_whole_schedule()
	{
		$service = new ScheduleService;
		$schedule = $service->getSchedule();
		$first_show = $schedule->first();
		$this->assertEquals($first_show->starting_hour, 1);
		$this->assertEquals($first_show->ending_hour, 1);
		$this->assertEquals($first_show->time(), '1 - 1 AM');
		$this->assertEquals($first_show->dayOfWeek(), 1);
	}
	*/

}
